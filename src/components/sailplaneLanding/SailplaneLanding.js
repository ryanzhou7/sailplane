
import React from 'react'

function SailplaneLanding () {
  return (
    <>
      do window.localStorage.setItem('currentComponent', 'app') in console and reload
      to checkout the app skeleton
    </>
  )
}

export default SailplaneLanding
